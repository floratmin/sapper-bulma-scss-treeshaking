# Integrate Sapper with SCSS, CSS framework and CSS tree shaking

This sapper setup is set up to showcase some problems related to css autoprefixer. We can see the error in the produced client-[hash].css
